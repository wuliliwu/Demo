import React, {Component} from 'react';
import {connect} from 'react-redux';
import {localStorageGetItem} from "../../../../../react-17.0.1/react-17.0.1/packages/react-devtools-shared/src/storage";



function mapStateToProps(state) {
  return {};
}

function mapDispatchToProps(dispatch) {
  return {};
}

const HYButton = (props) => {
  return
}

class UseCallback extends Component {
  render() {
    return (
      <div>

      </div>
    );
  }
}

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(UseCallback);